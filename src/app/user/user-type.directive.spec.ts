import { UserTypeDirective } from './user-type.directive';

describe('UserTypeDirective', () => {
  it('should create an instance', () => {
    const directive = new UserTypeDirective();
    expect(directive).toBeTruthy();
  });
});
